spring:
  application:
    name: order-service
  cloud:
    config:
      uri: http://localhost:8888
      username: ${spring.security.user.name}
      password: ${spring.security.user.password}
  security:
    user:
      name: admin
      password: password